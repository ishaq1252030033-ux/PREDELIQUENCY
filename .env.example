# Pre-Delinquency Intervention Engine - Environment variables
# Copy this file to .env and fill in values.

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/predelinquency

# API security
API_KEY=your-secret-api-key-here

# ML model path (relative to project root)
MODEL_PATH=ml/models

# Risk score threshold (0.0–1.0) for intervention
RISK_THRESHOLD=0.5

# Application (optional)
APP_NAME=Pre-Delinquency Intervention Engine
DEBUG=false
ENVIRONMENT=development
API_PREFIX=/api/v1
